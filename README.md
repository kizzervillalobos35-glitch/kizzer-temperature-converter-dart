# Kizzer Temperature Converter

A responsive temperature converter built in Flutter and Dart. It supports Celsius, Fahrenheit, and Kelvin, including absolute-zero validation.

## Run locally

Install [Flutter](https://docs.flutter.dev/get-started/install), then run:

```bash
flutter pub get
flutter run -d chrome
```

## Build for the web

```bash
flutter build web
```
